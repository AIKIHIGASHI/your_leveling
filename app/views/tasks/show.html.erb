<h1><%=@task.task_name%></h1>

<p>内容<%= simple_format @task.content%></p>
<p>作成日：<%=  @task.created_at.strftime("%Y-%m-%d")%></p>

<% if user_signed_in? && current_user.id == @task.user.id %>
<%= link_to "削除", task_path(@task.id), method: :delete%>
<% end %>

